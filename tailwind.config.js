// make a tailwind config file for nextjs with the following settings:
// - content: ["./src/**/*.{js,ts,jsx,tsx}"]

/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{js,ts,jsx,tsx}"],
  theme: {
    extend: {},
  },
  plugins: [],
};
// This is a basic Tailwind CSS configuration file for a Next.js project.
// It specifies the content paths where Tailwind CSS should look for class names to generate styles.
